# ml/requirements.txt
# Install with: pip install -r ml/requirements.txt

# ── Core ML stack ─────────────────────────────────────────────────────────────
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.2.0
joblib>=1.3.0

# ── Optional but recommended ───────────────────────────────────────────────────
xgboost>=2.0.0          # Adds XGBClassifier as a training candidate
lightgbm>=4.3.0         # Alternative boosting backend (not wired in by default)

# ── Visualisation (for confusion matrix & feature importance plots) ────────────
matplotlib>=3.8.0
seaborn>=0.13.0

# ── Data utilities ─────────────────────────────────────────────────────────────
scipy>=1.12.0           # Used internally by sklearn

# ── Dev / evaluation extras ───────────────────────────────────────────────────
tqdm>=4.66.0            # Progress bars (optional, used in evaluate.py)
tabulate>=0.9.0         # Pretty-print tables in CLI output
